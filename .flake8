# flake8 runs on every pull request, on both CI systems. It is the only linter
# in this repository on purpose: one tool, one config, nothing to explain twice.
[flake8]
max-line-length = 100
exclude =
    .git,
    .venv,
    __pycache__,
    build,
    dist,
    *.egg-info,
    notebooks
