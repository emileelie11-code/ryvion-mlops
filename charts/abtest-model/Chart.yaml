# apiVersion v2 rather than the inherited v1: v1 is the Helm 2 chart format, and
# although Helm 3 and 4 still read it, `helm lint` has warned about it for years.
apiVersion: v2
name: abtest-model
description: One version of the automobile MPG model, deployed with a colour label.
type: application
version: 0.2.0
appVersion: "0.1.0"
keywords:
  - mlops
  - blue-green
  - canary
  - shadow
